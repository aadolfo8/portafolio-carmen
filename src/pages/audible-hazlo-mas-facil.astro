---
import Layout from "../layouts/Layout.astro";

import { fade } from "astro:transitions";

const cabecera = {
  src: "AUDIBLE/1.gif",
  alt: "Cabecera",
};
const racional = {
  src: "AUDIBLE/2.png",
  alt: "Racional",
};
const graficas = {
  src: "AUDIBLE/3.png",
  alt: "Gráficas",
};
const metro = {
  src: "AUDIBLE/4.png",
  alt: "Metro",
};
const graficas2 = {
  src: "AUDIBLE/5.png",
  alt: "Graficas-2",
};
const revista = {
  src: "AUDIBLE/6.png",
  alt: "Revista",
};
const footer = {
  src: "AUDIBLE/7.png",
  alt: "Footer",
};
---

<Layout
  title="Audible - Hazlo más fácil"
  description="Encontrar una postura cómoda para leer a veces es muy complicado"
  favicon="AUDIBLE.png"
>
  <main
    class="main-project"
    transition:name={`image-audible-hazlo-mas-facil`}
    transition:animate={fade({ duration: "0.6s" })}
  >
    <section class="image-section">
      <img
        src={cabecera.src}
        alt={cabecera.alt}
        class="full-width-image"
        loading="lazy"
      />
    </section>

    <section class="image-section">
      <img
        src={racional.src}
        alt={racional.alt}
        class="full-width-image"
        loading="lazy"
      />
    </section>

    <section class="image-section">
      <img
        src={graficas.src}
        alt={graficas.alt}
        class="full-width-image"
        loading="lazy"
      />
    </section>

    <section class="image-section">
      <img
        src={metro.src}
        alt={metro.alt}
        class="full-width-image"
        loading="lazy"
      />
    </section>

    <section class="image-section">
      <img
        src={graficas2.src}
        alt={graficas2.alt}
        class="full-width-image"
        loading="lazy"
      />
    </section>

    <section class="image-section">
      <img
        src={revista.src}
        alt={revista.alt}
        class="full-width-image"
        loading="lazy"
      />
    </section>

    <section class="image-section">
      <img
        src={footer.src}
        alt={footer.alt}
        class="full-width-image"
        loading="lazy"
      />
    </section>
  </main>
</Layout>
